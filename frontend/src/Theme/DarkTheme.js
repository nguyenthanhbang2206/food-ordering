import { createTheme } from "@mui/material";

export const darkTheme = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: "#5A20CB",
        },
        secondary: {
            main: "#5A20CB",
        },
        background: {
            main: "#5A20CB",
            default: "#E6E6FA",
            paper: "#5A20CB",
        },
        textColor: {
            main: "#000000",
        },
    },
});